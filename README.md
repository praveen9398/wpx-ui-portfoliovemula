# WPX UI/UX Portfolio rebuild

Open `index.html` in a modern browser. The primary fonts and project imagery are bundled locally. The optional interactive footer game remains loaded from its original public script.
